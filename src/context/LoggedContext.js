import React, { createContext } from "react";

/**
 *  * isRequire
 *  define in Layout.js and WpSnackbar.js
 *  @param function handleShowSnackBar:*message,object:title,handleClick 
 *  @return 
 */
export const LoggedContext = createContext();